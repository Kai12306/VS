# C 踩坑速查表

> 都是老 baby 真踩过的，不是理论。
> 按「报什么错 → 怎么办」排的，报错先看**末尾那行红字**。
> 更新：2026-09-26

---

## 零、先记住这三条

| # | 记住这个 | 为什么 |
|---|---|---|
| 1 | **中文标点打进代码 = 必死**。出错了去看报错里有没有 `"` `，` `（）` → 100% 是全角 | 输入法默认中文，切不过来就中招（已复发 3 次以上） |
| 2 | **让代码不出东西，先查「它到底跑到了没有」**，别急着怀疑语法 | `return 0` 位置错会让后面的代码变死代码，编译器一声不吭 |
| 3 | **改完代码必须重新编译**。弹出「发生生成错误，是否继续?」→ 一律点 **否(N)** | 点「继续(C)」跑的是旧的程序，你会以为代码没生效 |

---

## 一、报错代码对照（C / LNK / 崩溃码）

| 报错 | 真因 | 怎么办 |
|---|---|---|
| **C2001** | 字符串没闭合，通常是全角引号 `"` 或中文标点 | 找那一行有没有全角符号。改完之后**不要动 `return 0` 那行**（C2143 是连带假错） |
| **C2143** | 一般是 C2001 的连带假错，不是真的少分号 | 假错，别去改代码 |
| **C2017 / C2146** | 转义符位置错了，或打成了全角 `\` | 反斜杠 `\` 必须是英文半角 |
| **C2065** | 忘了 `#include <stdio.h>` | 第一行加 `#include<stdio.h>` |
| **C4309** (⚠ 警告) | 常量截断，如 `short a = 32768;` | short 上界 32767，超了会绕回 -32768 |
| **C4996** (⚠ 警告) | VS 嫌 `scanf` 不安全 | 文件第一行加 `#define _CRT_SECURE_NO_WARNINGS 1` |
| **LNK2005** | 工程里有两个 `main` 函数 | 删掉多余的 main，一个工程只能有一个入口 |
| **0xC0000005** | 访问冲突。**典型：scanf 漏写 `&`** | `scanf("%d", &a)` —— `&a` 是取地址，漏了就崩 |
| **0xC00000FD** | 栈溢出 | 递归太深，或减少大数组的局部变量 |
| **退出码 0** | ✅ 程序是正常结束的 | 退出码 0 不是错误 |

---

## 二、VS 写 C 五条铁律

1. 模板选 **「空项目 / Empty Project」** ← 千万别选「控制台应用」
2. **命名分三处，只有一处带后缀**（2026-09-26 亲自确认）：

   | 位置 | 填什么 | 带后缀吗 |
   |---|---|---|
   | 解决方案名 | `c07` | ❌ 不带 |
   | 项目名 | `c07` | ❌ 不带（它会变成**文件夹名**） |
   | 新文件名 | `c07.c` | ✅ **必须带** |

   - 文件后缀决定编译器**用什么语法读它**：`.c` = C，`.cpp` = C++。考 C 就绝不能落成 `.cpp`
   - 最坑的差异：C 里 `malloc` 返回的 `void*` 可直接赋值，C++ 里**必须强转**
   - 副作用：VS 在 `.c` 里注释快捷键给 `/* */`，在 `.cpp` 里给 `//`
   - 30 秒自检：看「解决方案资源管理器」显示 `c07.c` 就是对的。显示 `.cpp` → 右键重命名改 `.c` → **重新 Ctrl+F5**（只改名不重编译会跑旧文件）
   - 「添加新项」里若有 `C 文件 (.c)` 模板，直接用它最省事；只有 `C++ 文件(.cpp)` 时再手动改后缀
3. **含中文的 .c 必须存 GB2312 / 代码页 936**
   - 在右下角状态栏点当前编码 → 选带「保存」那条
   - ⚠️ **新建 .c 默认 UTF-8 且不会继承上一份**，每个新文件都要重设一次
   - VS 2022+ 已经没有「高级保存选项」了，别找
4. 「发生生成错误。是否继续…」 → 一律点 **否(N)**
5. 运行用 **Ctrl+F5**（开始执行不调试）。崩了 → `Shift+F5` 停 → 关掉自动打开的系统头文件标签 → 再 `Ctrl+F5`

---

## 三、他反复踩过的（按复发次数排）

### ⭐⭐⭐ 全角 / 中文标点（复发 3 次以上）
- 报错里出现 `"`、`，`、`（）`、`；` → 一定是全角
- 打代码前先切英文输入法：`Win + 空格`，或设置微软拼音「中文时使用英文标点」
- **根治**：微软拼音 → 「中文时使用英文标点」打开，一劳永逸
- **判据**：转义序列（`\n` `\r\n` `\t` `\\` `\"`）永远住在**引号内部**；位置才是判据，颜色不算

### ⭐⭐⭐ 改完不编译，直接点「继续 (C)」
- 结果：跑的是上次的 exe，你会以为「我改了怎么没变化」
- 正确：`Ctrl+S` 保存 → **重新 Ctrl+F5 编译运行**

### ⭐⭐⭐ `return 0;` 位置错（2026-09-26 今晚才踩）
- `return 0;` 必须是 `main` 的**最后一句**
- 放在函数中间 → 程序到那儿就结束，后面的代码全是**死代码**
- 表现：语法全对、编译过、零警告、退出码 0、**控制台什么也不输出**
- 这就是最难查的一类：**没报错，但结果是错的**

### ⭐⭐ ⚠️ 警告比错误更阴
| 类别 | 编译器 | 你 |
|---|---|---|
| 错误（C 开头 / LNK 开头） | ❌ 编不过 | 拦住你了，安全 |
| **警告（带 ⚠）** | ✅ 能过 | **结果可能是错的，更危险** |
| **逻辑错（无错无警告）** | ✅ 能过 | **退出码还是 0**，最难查 |

盯状态栏那个 ⚠ 数字，别只盯着报错列表。

### ⭐⭐ 「项目」≠「文件夹」≠「文件」（栽过 2 次）
- 每个练习都要走 `文件 → 新建 → 项目`
- 导航栏最左那个下拉框是**项目名**，不是文件名
- 临时解：`Ctrl+Alt+L` → 右键 → `从项目中排除`
- exe 在 `D:\vs2026\<解决方案>\x64\Debug\<项目名>.exe`

### ⭐⭐ 注释
- 优先用 `//`，别用 `/* */`（忘了写 `*/` 会把后面一大片都注释掉）
- ⚠️ **VS 在 `.c` 文件里给 `/* */` 提示，在 `.cpp` 里给 `//` 提示** → 别纠结，手打 `//`
- 注释解释「**为什么**」，不要解释「是什么」

---

## 四、printf / scanf 铁律

- **加引号 = 原样输出**；不加引号 = 输出变量的值
- 格式符取决于**后面给的东西的类型**：`%d` 整数、`%f` 浮点、`%c` 字符、`%s` 字符串、`%p` 地址
- `\n` 是换行，**在引号里面**
- `scanf` 的每一项前面都**必须有 `&`**：`scanf("%d%d", &a, &b)`
- 写了 `scanf` 的程序，**黑窗口会停住等你输入**，打完数字回车
- 用 scanf 前先加 `#define _CRT_SECURE_NO_WARNINGS 1`，否则 VS 会拿 C4996 一直烦你

---

## 五、那些「看起来坏了其实没坏」的事

| 现象 | 真相 |
|---|---|
| GitHub 网页打不开 | 国内常态 ≠ 操作失败。看仓库内容去 API：`api.github.com/repos/Kai12306/VS/commits` |
| 微信窗口 `×` 了 | 那是**最小化到托盘**，进程一个没死，还在吃 600MB。`Weixin` ×6 就是它 |
| 任务结束任务后又起来了 | 看门狗：微信本体拉起 `WeChatAppEx`，`nvcontainer` 拉起 NVIDIA Overlay。**杀进程没用，得断源头** |
| 任务管理器里找不到「禁用」 | 「禁用」只在左侧**「启动应用」**页，「结束任务」才在「进程」页 |
| 注释命令给了错的符号 | 见上面第三节，手打 `//` |
| 文件夹时间跟我改代码的时间对不上 | 那是**文件夹**自己的时间（`D:\VS\.vscode` 是 VS Code 自动生成的），不是文件的时间 |
| 「选择文件夹」框里看不到 `.py` | 这个框按设计**只列文件夹、隐藏所有文件**。想看文件用普通资源管理器 |
| 蓝色字不知道能不能点 | 鼠标移上去变**小手**就是链接，能点 |
| 改代码前想搜 VS 菜单项 | 菜单项带「当前对象名」前缀，**看尾巴别看开头** |

---

## 六、IntelliSense 与 Copilot（9/20 教训）

- **IntelliSense（代码补全）留着**，它帮的是样板层：`#include` / `main` / `return 0`
- **Copilot 关掉**，它会「猜」你想写什么，让你产生「我会了」的错觉
- 9/20 那天你 day1.py 的代码就是 Copilot 填的，你以为是自己写的
- 关法：状态栏 Copilot 面板 → Inline Suggestions 展开 → `Ghost text suggestions` / `Ghost text suggestions for Python` / `Next edit suggestions` **三个勾全取消**

---

## 七、学习上的三条（比语法更重要）

1. **看懂 ≠ 会写。** 看完视频必须自己敲一遍，卡住就回看，别复制粘贴
2. **保存 ≠ 运行成功。** 「写好代码」和「跑起来看见结果」是两件事
3. **在编辑器里打字 ≠ 在终端里打字。** 有行号 1、2、3 的地方是代码文件；没有行号、有 `PS D:\...>` 的地方才是终端（这条是 Python 时期的事，同样适用）

---

## 八、环境

- **主力**：VS Community 2026（中文+浅色）+ MSVC，在 `D:\develop\vs2026\Community`
- **练习工程**：`D:\vs2026\cNN\`（注意 ≠ 仓库 `D:\VS`，两个都要查）
- **同步**：`D:\VS\c\cNN.c` ← `D:\vs2026\cNN\...\cNN.c`，进仓库前转 UTF-8
- **备用**：Dev-C++ 5.11（TDM-GCC 4.9.2），跟 MSVC 报错话术**完全不一样**，别两套混着学
- ⚠️ 同目录 `DevC安装助手.exe` 是套壳流氓，删掉

---

## 九、一头雾水时的排查顺序

1. 报错列表里有没有 `"` `，` `（）` → 有就是全角标点
2. 编码对不对（右下角状态栏）→ 含中文必须是 GB2312
3. `return 0` 是不是在最后一行
4. 改完有没有重新 `Ctrl+F5`
5. 还不行 → 把**完整的错误原文**（不是描述）发出来
